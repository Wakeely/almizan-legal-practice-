"use client";

// =============================================================================
// SubscriptionPaywallModal — STUB
// -----------------------------------------------------------------------------
// The reference LandingPage imports this component but does not actually
// render it in the current tree. To keep the faithful port intact, we provide
// a no-op stub here. The real paywall UI (tier comparison, upgrade flow,
// billing-cycle toggle, trial countdown) will be ported in Turn 5 of the
// rollout plan.
// =============================================================================

import React from "react";

export default function SubscriptionPaywallModal() {
  // Intentionally renders nothing — see header comment.
  return null;
}
